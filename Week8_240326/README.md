# Week 8 React Router Demo

This project demonstrates a basic React application with:
- React Router for navigation
- `useRef` to manage the input field
- `useState` hook for item list state
- `key` attributes for list rendering

## Run
1. Install dependencies with `npm install`
2. Start the app with `npm run dev`

Open `http://localhost:5173` in your browser after the server starts.
